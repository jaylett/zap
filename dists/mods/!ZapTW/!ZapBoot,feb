| !ZapBoot file for ZapTaskwindow

| Claim task windows if not already claimed
Obey <Obey$Dir>.!SetUp
IfVarSet Alias$Taskwindow Then Else Set Alias$Taskwindow "Run <ZapTaskwindow$Dir>.Taskwindow "
IfVarSet TaskWindow$Server Then Else Set TaskWindow$Server <Zap$Dir>
