| Cycle through the 'C' based modes
#IfModeN Pascal
 MODEN "C"
#Else
 #IfModeN Java
  MODEN "Pascal"
 #Else
  #IfModeN C++
   MODEN "Java"
  #Else
   #IfModeN C
    MODEN "C++"
   #Else
    MODEN "C"
   #EndIf
  #EndIf
 #EndIf
#EndIf
