| !ZapBoot file for ZapBASIC

| Claim task windows if not already claimed
Set ZapBASIC$Dir <Obey$Dir>
IfVarSet Alias$Taskwindow Then Else Set Alias$Taskwindow "Run <ZapBASIC$Dir>.Taskwindow "
IfVarSet TaskWindow$Server Then Else Set TaskWindow$Server <Zap$Dir>
