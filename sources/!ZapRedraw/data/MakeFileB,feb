| Runs the Zap Make file with the correct options to make all of Zap nicely.

| create header files
Set prefix$dir <MakeZapRedraw$Dir>
AMU -f <MakeZapRedraw$Dir>.MakeFileA
| compile code
Set prefix$dir <MakeZapRedraw$Dir>
AMU -f <MakeZapRedraw$Dir>.MakeFileB
